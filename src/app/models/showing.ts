export class Showing{
  description: string;
  time: string;
  address: string;

  constructor(description: string, time: string, address: string) {
    this.description = description;
    this.time = time;
    this.address = address;
  }
}